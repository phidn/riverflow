---
updated: YYYY-MM-DD
progress: "<n>/<m> themes shipped"   # freshness signal + at-a-glance gauge
---

# Roadmap: <product>

> The living, forward-looking page for one **product**: where it is heading and how far along it
> is. **Optional by scale** — a single-feature product is served by its `product-brief`; a product
> spanning multiple themes across cycles needs this. **Overwritten in place** (like the wiki):
> refresh the theme status + frontmatter whenever a plan under a theme ships.

## North star

The stable destination — what this product is ultimately for. Rarely changes.

## Now / Next / Later

The themes between here and the north star, ordered by horizon. Each theme carries a status
(`planned | in-progress | shipped`) and links the plans that `Serves:` it.

### Now
- **<theme>** — in-progress — ← plan-0005, plan-0007

### Next
- **<theme>** — planned

### Later
- **<theme>** — planned

## Gap-to-vision

Where the product is today versus the north star — the honest delta. This is the **source that
refills the backlog**: when the backlog runs dry (or the north star changes), diff this against the
wiki (as-built) and graduate new backlog items from the gap.
